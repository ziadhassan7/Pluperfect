import 'package:flutter/material.dart';

class QuoteProgressBar extends StatelessWidget {
  const QuoteProgressBar({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
